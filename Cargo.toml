[package]
name = "stahl-interpreter"
version.workspace = true
authors = ["mattwparas <matthewparas2020@u.northwestern.edu>"]
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/TeglonLabs/Stahl"
description = "Stahl repl and command line interface"
default-run = "stahl"

include = ["/src/**/*", "/Cargo.toml", "/README.md", "LICENSE*"]

[workspace.package]
version = "0.6.0"

[[bin]]
name = "stahl"
path = "src/main.rs"

[workspace.dependencies]
# This has to line up with the workspace version above
stahl-core = { path = "./crates/stahl-core", version = "0.6.0", features = ["dylibs", "markdown", "stacker", "sync", "rooted-instructions"] }

[features]
default = ["mimalloc"]
build-info = ["vergen"]
mimalloc = ["dep:mimalloc"]
# Note: It does not appear that this will get propagated to any crate that depends on
# the workspace feature. This is unfortunate, since we'd like everything to actually
# use the workspace dependency. For now, if you want to test with sync, you should
# just add the sync flag directly.
# default = ["sync"]
# sync = ["stahl-core/sync"]

[dependencies]
once_cell = "1.17.0"
stahl-core = { workspace = true }
stahl-derive = { path = "./crates/stahl-derive", version = "0.5.0" }
stahl-repl = { path = "./crates/stahl-repl", version = "0.6.0" }
env_logger = "0.10.0"
log = "0.4.17"
clap = { version = "=4.1.4", features = ["derive"] }
stahl-doc = { path = "./crates/stahl-doc", version = "0.6.0" }
mimalloc = { version = "0.1.43", optional = true }
rost = { git = "https://github.com/michidk/rost.git" }

[target.'cfg(not(target_os = "redox"))'.dependencies]
cargo-stahl-lib = { path = "./crates/cargo-stahl-lib", version = "0.1.0" }


[dev-dependencies]
serde = { version = "1.0.152", features = ["derive"] }

[workspace]
members = [
    ".",
    "crates/*", 
    "libs/*",
]

[profile.release]
debug = true
lto = true

[profile.test]
opt-level = 2

[build-dependencies]
vergen = { version = "9.0.0", features = [ "build", "cargo", "rustc" ], optional = true}
